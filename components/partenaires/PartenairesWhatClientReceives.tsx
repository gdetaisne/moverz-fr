"use client";
import { motion } from "framer-motion";
import { LayoutList, Shield, Filter } from "lucide-react"; export default function PartenairesWhatClientReceives() { const items = [ { icon: LayoutList, text: "Une comparaison lisible des 3 à 5 offres" }, { icon: Shield, text: "Des repères pour décider sereinement : prestations, garanties, avis, sérieux perçu" }, { icon: Filter, text: "Une sélection « encadrée » : le client ne part pas dans tous les sens" } ]; return ( <section className="relative py-20 md:py-32 bg-white"> <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8"> <motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="text-center mb-12" > <h2 className="text-3xl md:text-5xl font-heading font-bold mb-4 text-[var(--color-text)]"> Ce que reçoit le client </h2> </motion.div> <div className="space-y-4"> {items.map((item, i) => ( <motion.div key={i} initial={{ opacity: 0, x: -20 }} whileInView={{ opacity: 1, x: 0 }} viewport={{ once: true }} transition={{ delay: i * 0.1 }} className="flex items-center gap-4 p-6 rounded-2xl bg-gradient-to-r from-[#F9FAFB] to-white border border-[var(--color-border)]" > <div className="flex-shrink-0 flex items-center justify-center w-12 h-12 rounded-xl bg-[var(--color-accent)]/10"> <item.icon className="w-6 h-6 text-[var(--color-accent)]" /> </div> <p className="text-[var(--color-text-secondary)] font-medium"> {item.text} </p> </motion.div> ))} </div> </div> </section> );
}
