"use client";
import { motion } from "framer-motion";
import { UserCheck, Phone, FileEdit, Calendar } from "lucide-react"; export default function PartenairesIfChosen() { const steps = [ { icon: Phone, text: "appel / visio / visite technique" }, { icon: FileEdit, text: "ajustements du devis si nécessaire" }, { icon: Calendar, text: "confirmation et organisation du déménagement" } ]; return ( <section className="relative py-20 md:py-32 bg-gradient-to-b from-[#F9FAFB] to-white"> <div className="max-w-5xl mx-auto px-4 sm:px-6 lg:px-8"> <motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="text-center mb-12" > <div className="inline-flex items-center justify-center w-16 h-16 rounded-2xl bg-[var(--color-accent)]/10 mb-6"> <UserCheck className="w-8 h-8 text-[var(--color-accent)]" /> </div> <h2 className="text-3xl md:text-5xl font-heading font-bold mb-6 text-[var(--color-text)]"> Si le client vous choisit </h2> <p className="text-lg text-[var(--color-text-secondary)] mb-8"> Moverz vous met en relation directement avec le client. </p> </motion.div> <motion.div initial={{ opacity: 0, y: 20 }} whileInView={{ opacity: 1, y: 0 }} viewport={{ once: true }} className="bg-white rounded-3xl p-8 md:p-12 border border-[var(--color-border)] shadow-sm" > <p className="text-[var(--color-text-secondary)] mb-6"> Ensuite, vous prenez la main comme d'habitude : </p> <ul className="space-y-4 mb-8"> {steps.map((step, i) => ( <motion.li key={i} initial={{ opacity: 0, x: -20 }} whileInView={{ opacity: 1, x: 0 }} viewport={{ once: true }} transition={{ delay: i * 0.1 }} className="flex items-center gap-4" > <div className="flex-shrink-0 flex items-center justify-center w-10 h-10 rounded-lg bg-[var(--color-accent)]/10"> <step.icon className="w-5 h-5 text-[var(--color-accent)]" /> </div> <span className="text-[var(--color-text-secondary)]">{step.text}</span> </motion.li> ))} </ul> <div className="p-5 rounded-xl bg-gradient-to-r from-brand-turquoise/10 to-[#A8E8E8]/10 border border-[var(--color-accent)]/30"> <p className="text-[var(--color-text)] font-semibold"> 👉 À ce stade, vous n'êtes plus en concurrence : le client vous a choisi. </p> </div> </motion.div> </div> </section> );
}
